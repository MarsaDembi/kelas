/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['via.placeholder.com'], // ✅ tambahkan ini
    },
  };
  
  module.exports = nextConfig; // ✅ WAJIB ada untuk membuat config aktif
